import type { RequestOptions } from '../typings/chatgptService'

export async function chatReplyProcess(options: RequestOptions) {
  // const { message: Send, lastContext, process, systemMessage } = options
}
